"use client";
import PageWrapper from "@/shared/components/PageWrapper/PageWrapper";
// import { useProductsHook } from "./useProductsPage.hook";
// import { ProductCard } from "@/shared/components/ProductCard";
// import { Box } from "@mui/material";

export default function Products() {
  // const { productsData } = useProductsHook();

  return (
    <PageWrapper>
      {/* 
        - categories cards with their images when clicked it will redirect the user to the category products
      */}
      <div>Categories cards</div>
    </PageWrapper>
  );
}

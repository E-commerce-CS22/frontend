import { makeStyles } from "@mui/material";

export const useCustomAccordionStyles = makeStyles({
  root: {
    borderRadius: "15px !important",
  },
  questionText: {
    fontSize: 16,
    fontFamily: "CoHeadlineTrial-Light,Arab Kufi Regular",
  },
  summaryHeight: {
    height: 60,
  },
});
